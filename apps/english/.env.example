# The frontend bundle is config-free — it bakes in NO environment variables.
# All configuration lives on the server (see server/.env.example). The browser
# fetches what it needs at runtime via GET /api/config.
#
# In dev, the Vite dev server proxies /api/* to the Express server (see
# vite.config.js), so the frontend needs nothing here.
